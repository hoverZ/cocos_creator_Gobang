
let aStart;

export default class AStar{

    constructor(){
        if( aStart ){
            return aStart;
        }
        aStart = this
    }

    /**
     * 计算胜出权重
     */
    computeWinWeight( gameData ){

    }

}